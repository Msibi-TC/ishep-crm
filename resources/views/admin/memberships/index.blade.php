@extends('layouts.app')
@section('content')<div class="container py-5"><h1>Membership application queue</h1><form class="row g-2 mb-3"><div class="col-md-4"><input class="form-control" name="status" placeholder="Filter status" value="{{ request('status') }}"></div><div class="col"><button class="btn btn-outline-primary">Filter</button></div></form><div class="list-group">@forelse($applications as $a)<a class="list-group-item" href="{{ route('admin.memberships.show',$a) }}">{{ $a->reference_number }} · {{ $a->user->name }} · {{ $a->membershipType->name }} · {{ $a->status->value }}</a>@empty<div class="alert alert-light">No applications.</div>@endforelse</div>{{ $applications->links() }}</div>@endsection
