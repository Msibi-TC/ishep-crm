@extends('layouts.app')
@section('content')<div class="container py-5"><div class="d-flex justify-content-between"><h1>My membership</h1><a class="btn btn-primary" href="{{ route('member.applications.create') }}">Start or resume application</a></div>@if($membership)<div class="alert alert-info mt-3">Membership: <strong>{{ $membership->status->value }}</strong>@if($membership->membership_number) · {{ $membership->membership_number }}@endif</div>@endif<div class="list-group mt-3">@forelse($applications as $a)<a class="list-group-item list-group-item-action" href="{{ route('member.applications.show',$a) }}"><strong>{{ $a->reference_number }}</strong> <span class="badge text-bg-secondary">{{ $a->status->value }}</span></a>@empty<div class="alert alert-light">No applications yet.</div>@endforelse</div></div>@endsection
